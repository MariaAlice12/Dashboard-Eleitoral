import { redirect } from 'next/navigation'

// Placeholder temporário: a home com os botões das seções (Federais, Estaduais,
// Senadores, Vereadores, Linha do Tempo Presidencial) é a próxima etapa.
export default function HomePage() {
  redirect('/federais')
}
